const Page = () => {
    return (
        <div>
            <h1>This is the user page</h1>
        </div>
    );
};

export const generateMetadata = () => {
    return {
        title: "User Page"
    };
};

export default Page;
